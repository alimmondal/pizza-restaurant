import styles from '../styles/Add.module.css';

const AddButton = () => {
    return (
        <div className={styles.container}>
            Add New pizza
        </div>
    );
};

export default AddButton;